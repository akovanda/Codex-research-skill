apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: research-registry
spec:
  rules:
    - host: research-registry.example.internal
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: research-registry
                port:
                  number: 80
