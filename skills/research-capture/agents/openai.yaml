interface:
  display_name: "Research Capture"
  short_description: "Deprecated explicit research-capture adapter"
  default_prompt: "Use the deprecated $research-capture compatibility adapter explicitly."
policy:
  allow_implicit_invocation: false
